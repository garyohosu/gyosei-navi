# note記事 下書き（Phase 0 実機検証）

> ## 🚫 公開不可（2026-08-17）
>
> **この下書きは公開してはならない。**
>
> 本文の数値・バージョン・ログは旧 `validation-results.md` を出典としているが、その記録には一次証跡がなく、
> 記載された環境バージョンが実機と一致しないことが判明した（`EVIDENCE_AUDIT.md` 参照）。
>
> その後、実行ログ付きで再検証を行った。**データ系の数値（75,071件／3,203KB／相続1,614件／25グループ／
> 国土交通省13,645）は再現したので、そのまま使える。** 一方、以下は差し替えが必要。
>
> - **環境バージョン**: 記事本文の「uv 0.6.12 / Python 3.14.0」は誤り。実測は uv 0.6.11 / Python 3.12.10。
> - **MCP接続の記述**: 本文はClaude Code経由の話（`claude -p`、Pydantic検証エラー、API利用料0.65ドル、
>   ツール呼び出し順）を書いているが、**再検証ではLLMを介さないstdioクライアントで確認しており、
>   この部分は未再現。** 書くならClaude Codeで実際にやり直すこと。
> - **`setup.sh` 単体での完走（A12）**: 再検証では手動コマンドで進めたため未再現。
> - **`apcli preview` / Chrome内蔵AI**: 再検証でも未実施。
>
> **上記を直すまで公開しない。** 文章の構成・文体はそのまま再利用してよい。

作成: 2026-08-17 / 出典: `validation-results.md`（**未検証**）

---

## タイトル案

1. デジタル庁の行政手続MCPサーバを、Windowsの個人PCで動かしてみた
2. 行政手続7万5000件のMCPサーバ、本当に「AIが計算していない」のか確かめた
3. デジタル庁のMCPサーバを試したら、集計はちゃんとサーバー側でやっていた
4. 行政手続75,071件。デジタル庁のMCPサーバを実機で検証した記録
5. デジタル庁のMCPサーバ、Windowsだと最初に文字化けする

推し: 2（記事の一番の確認ポイントがタイトルに出る）。無難に行くなら1。

## 有料パートの切り分け案（任意）

- 無料: 導入 〜「LLMなしでも検索も集計もできる」まで。ここまでで「動く／動かない」の結論は出る。
- 有料: 「AIは本当に計算していないのか」以降。CLIとMCPの結果一致で裏を取る手順、Pydanticで弾かれたログ、dataset.yamlの読みどころ、未確認だった項目。
- ただし今回は初回なので全文無料でもよい。検証手順そのものが読者の再現材料になる。

---

# 本文

デジタル庁が「行政手続データ分析 MCP Server」を公開したという記事を読んだ。

行政手続の棚卸調査、約7万5000件をMCPサーバ経由でAIに触らせる、という話。MITライセンスで、GitHubにそのまま置いてある。

面白かったのは「AIに生データを渡して計算させない」と書いてあったところ。AIは検索条件を指定するだけで、集計はサーバー側でやる、と。

本当にそうなっているのか。あと、Windowsの個人PCで普通に動くのか。

試した。

## セットアップは3行

公式リポジトリはこれ。

https://github.com/digital-go-jp/administrative-procedures-mcp

READMEどおりだと `git clone` して `cd` して `./setup.sh` の3行で終わる。私はGit Bashで `setup.sh` も動かしてみたが、実際の検証は手動コマンドで進めた。

```bash
uv sync --extra excel
uv run apcli fetch procedures-survey-r6
```

`uv sync` で76パッケージが入る（fastmcp 3.4.5、polars 1.43.2 など）。

`apcli fetch` はデジタル庁の配布ページからExcelを取ってきてParquetに変換する。

- 取得元: https://www.digital.go.jp/resources/procedures-survey-results
- 落ちてきたxlsx: 14.0MB
- 変換後のParquet: 3,203KB
- レコード数: **75,071件**

記事にあった「約7万5000件」「Parquet約3MB」は実測と合っていた。

環境はWindows 11、uv 0.6.12、Pythonはuvが管理している3.14.0。データを含めても1GBも使わなかった。

## Windowsだと最初に文字化けする

`uv run apcli list` を叩いたら日本語が全部化けた。

コンソールのコードページが932（Shift-JIS）のまま、UTF-8の出力を解釈しているせい。`chcp 65001` はパイプ経由の実行では効かなかった。

これで解決した。

```bash
PYTHONIOENCODING=utf-8 uv run apcli list
```

以降は全部この形で実行している。今回見つかったWindows固有の詰まりどころは、結局これ1つだけだった。

## LLMなしでも検索も集計もできる

このリポジトリ、`apcli` というCLIが付いていて、AIもAPIキーもなしで中身を触れる。

```bash
PYTHONIOENCODING=utf-8 uv run apcli query procedures-survey-r6 -q 相続 --limit 5
```

「相続」で **1,614件** ヒットした。日本語の全文検索がそのまま通る。`--limit 5` で5件に絞られて、`next_cursor` でページも送れる。

集計はこちら。

```bash
PYTHONIOENCODING=utf-8 uv run apcli summarize procedures-survey-r6 -g 所管府省庁 -m count
```

所管府省庁別で **25グループ**。上位は国土交通省13,645、厚生労働省10,504、経済産業省8,577。

同じコマンドを2回叩いて、同じ数字が返ることも確認した。当たり前のようだが、ここが再現しないと後の検証が全部あやしくなる。

出力には `provenance` が付いてくる。データセット名、公開日（2025-07-24）、取得日時、`source_url`、発行者。出典が結果と一緒に返ってくるのはありがたい。

## dataset.yaml が思ったより読ませる

データの意味は `datasets/procedures-survey-r6/dataset.yaml` に書いてある。38フィールド分の定義ファイル。

単なる「カラム名と型の一覧」ではなかった。

たとえば「オンライン手続件数」にはこう書いてある。

> null（欠損）は「件数不明」を意味する。0 は基本的に「オンライン手続なし」だが、地方等で件数集計が困難な一部の手続では 0 と記録されている場合がある。

0と欠損が違う意味だ、というのが定義レベルで書いてある。これを知らずに平均を出したら普通に間違える。

数値の但し書きもある。「総手続件数」は有効数字2桁以上の概数、「オンライン手続件数」は1〜2桁程度の概数で一部試算値を含む、と。

コード値も、選択肢ごとに制度上の意味まで書いてある。コード⇔ラベルの対応表ではない。

`computed_measures` に「オンライン率」の定義（どのフィールドのどの値を数えるか）が入っていて、これも `apcli inspect` の出力にそのまま出てくる。

「AIの推論精度を上げる」のではなく、間違えやすい判断を先にファイルへ落としておく、という作りに見える。

## で、AIは本当に計算していないのか

ここが一番確かめたかったところ。

Claude Codeから `.mcp.json` 経由でMCPサーバに繋いで、こう聞いた。

```text
所管府省庁別に手続件数を集計して
```

呼ばれたツールと引数まで全部ログに残した。

1. `list_datasets` `{}`
2. `inspect_dataset` `{"dataset_id": "procedures-survey-r6"}`
3. `summarize_records` に `metrics` をオブジェクトの配列で渡した → **サーバー側で拒否**（Pydanticのvalidation error 3件）
4. ツール定義を読み直して `metrics: ["count", "sum:総手続件数"]` で再実行 → 成功

最終結果は `total_group_count: 25`。

さっきCLIで直接叩いたときと**完全に同じ25グループ**だった。国土交通省13,645も一致。

LLMが渡したのは `group_by` と `metrics` という条件だけ。それでCLI直叩きと同じ数字が返ってきた。

厳密に言えばこれは「集計がサーバー側で動いている」ことの直接証明ではなくて、LLMを挟んだ場合と挟まない場合で結果が一致した、という事実の確認になる。ただ、AIが生データを見て数えているならこう毎回きれいには揃わないはずで、実務的にはここまで確認できれば信用していいと思う。

3番の「型が違うから弾かれた」も面白い。LLMが勝手な形で集計しようとしたらサーバーが拒否する。生データを渡して手計算させる余地がない、という補強にはなる。ただしこれ自体は「集計がサーバー側で動く証拠」ではないので、あくまで補強材料。主根拠はCLIとの結果一致のほう。

もう1問、「厚生労働省が所管する行政手続を検索して」も投げた。10,504件（全75,071件中）と返ってきて、回答の末尾にこう付いていた。

> 本データは政府の公式見解ではありません。件数フィールドは概数・試算値を含みます。

出典と注意書きが自動で付く。これは自分でツールを作るときに真似したい。

ちなみに2問でAPI利用料は合計0.65ドルくらいだった。

## 確認できなかったこと

正直に書いておく。

`apcli preview` でローカルUIは起動した。http://127.0.0.1:8765/ に立って、日本語も化けずに表示された。127.0.0.1 にしかバインドされていないことも確認済み。

ただ、**Chrome内蔵AI（Prompt API / Gemini Nano）の実際の動作は確認できていない**。ヘッドレスのChromeでスクリーンショットを撮ったら、画面右上が「内蔵AIを確認中…」のまま止まっていた。モデルのダウンロードやフラグの有効化が要るはずで、これは手でブラウザを開いて確かめるしかない。

同じ理由で、MCP Apps対応クライアントでグラフや表が実際に描かれるところも未確認。テキスト出力モードのClaude Codeでは評価できない。

あとHTTPモードを起動していないので、応答のMCP仕様バージョン（`/health`）も見ていない。

記事から拾った検証可能な主張は16個あって、うち14個は実機で一致を確認できた。残り2つが、上に書いたMCP Apps UIの描画と、デジタル庁の調査手法そのものに関する話（実装を読んでも真偽が判断できないのでスコープ外にした）。

## おまけ：ライフイベントの分類が既に入っていた

`手続が行われるイベント(個人)` というフィールドを見ていたら、中身がこうなっていた。

> 妊娠、出生・こども、引越し、就職・転職、結婚・離婚、医療・健康、税金、年金の受給、死亡・相続

引っ越し、結婚、出産、相続、退職。一般の人が行政手続を調べたくなるタイミングが、もうデータ側に分類として入っている。

これは検証の本題ではないのだけど、次に何か作るなら一番効きそうな情報だった。

## まとめ

- 個人のWindows PCで、公式手順どおりに動いた
- 詰まったのは文字化けだけ。`PYTHONIOENCODING=utf-8` で解決
- APIキーなしでもCLIで検索・集計はできる
- 「AIは計算していない」は、CLI直叩きとMCP経由で集計結果が一致することまで確認できた
- Chrome内蔵AIまわりは未確認。ここは手でブラウザを開く必要がある

まずは `apcli` だけ動かしてみるのがおすすめです。AIを繋がなくても、dataset.yaml を読むだけでけっこう面白い。

---

※記事中のバージョン・件数・ファイルサイズはすべて2026-08-17時点の実測値です。公式リポジトリは技術検証を目的としたサンプルコードと明記されており、継続的な保守は保証されていません。
