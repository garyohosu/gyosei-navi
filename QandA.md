# QandA

VALIDATION_PLAN.md レビュー時の不明点・要判断事項。公式リポジトリ digital-go-jp/administrative-procedures-mcp のREADME（2026-08-17時点）と照合済み。即修正できた項目はVALIDATION_PLAN.mdへ反映済みで、ここには含めない。

## 2026-08-17 VALIDATION_PLAN.md レビュー

### Q1. Claude Codeから公式MCPへ接続する前に、`.mcp.json` / `setup.sh` / サーバーコードを読むレビュー工程を入れるか
- 状態: 解決済み（2026-08-17）
- 決定: 実行前レビューを行う。Step 0で `setup.sh`、`.mcp.json`、そこから起動されるコマンド、主要処理、データ取得先を実行前に確認する。WORK_INSTRUCTION.mdに反映済み。

### Q2. 検証結果の記録ファイル名を確定するか
- 状態: 解決済み（2026-08-17）
- 決定: `validation-results.md` をgyosei-naviリポジトリ直下に作成し記録する。WORK_INSTRUCTION.mdに反映済み。

### Q3. 「十分な空き容量」の具体的な閾値
- 状態: **保留に差し戻し（2026-08-17、Q11参照）**。以下の実測値には証跡がなく、根拠として使えない
- 実測: `uv sync --extra excel` 後の依存パッケージ（polars-runtime含む）約数百MB、取得データ（xlsx原本14.0MB＋Parquet 3.2MB）を合わせても1GB未満で収まった。検証時のC:ドライブ空き容量は約44GBあり全く問題にならなかった。
- 決定: **目安として「2〜3GB程度の空き」を明記する。** VALIDATION_PLAN.md Step 0に反映済み。実測1GB未満に対して余裕を持たせた値であり、厳密な最小要件ではなく目安として扱う。

### Q4. 停止条件にある「代替環境」の範囲
- 状態: 解決済み（2026-08-17）
- 背景: 第6節の停止条件で「Windows固有の問題が未解決で、代替環境でも検証できない」とあるが、「代替環境」がWSL/Git Bashまでを指すのか、別マシン（Mac/Linux実機・VM）まで含むのかが未定義だった。
- 決定: **同一PC上のWSLおよびGit Bashまでとする。** 別マシン（Mac/Linux実機・VM）の調達は本検証のスコープに含めない。WSL／Git Bashでも解決しない場合は停止条件に該当したものとして扱う。VALIDATION_PLAN.md 第6節に反映済み。

### Q5. Step 6の質問例を公式READMEの利用例に寄せるか
- 状態: 解決済み（2026-08-17）
- 背景: 公式READMEには「オンライン未実施の手続にはどんな傾向があるか」「優先的に見直し候補になりそうな行政手続を探して」等の利用例が別途載っている。VALIDATION_PLAN.mdのStep 6は独自の4問のみ。
- 決定: **不採用（追加しない）。** Step 8で実行した2問により、ツール名・引数・サーバー側集計の一致（CLI結果との突き合わせ）まで確認できており、Phase 0の目的には足りている。公式例の追加はPhase 1でUI設計の題材が必要になった時点で再検討する。

### Q6. MCP仕様2026-07-28（FastMCP 4系プレリリース）での動作確認も範囲に含めるか
- 状態: 解決済み（2026-08-17）
- 背景: 公式READMEには既定の2025-11-25に加え、プレリリースのFastMCP 4系を追加インストールすれば2026-07-28仕様でも動く旨の記載がある。
- 決定: **対象外とする。** Phase 0の目的は記事執筆時点の既定構成（FastMCP 3系・MCP仕様 2025-11-25）で記事と公式実装の一致を確認することにあるため。VALIDATION_PLAN.md 第2節に「対象外」として明記済み。

### Q7. `apcli` コマンドの実行方法（venv activate か `uv run apcli` か）を統一するか
- 状態: 解決済み（2026-08-17）
- 決定: `uv run apcli ...` に統一する。WORK_INSTRUCTION.mdの全コマンド例に反映済み。

### Q8. innovaTopia記事側の主張を先に列挙するか
- 状態: 解決済み（2026-08-17）
- 決定: 実機検証前に記事本文から検証可能な主張を抽出し、公式READMEの記載と区別して記録する。最低限の照合対象10項目をWORK_INSTRUCTION.mdに列挙済み。各項目は「記事の主張 / 公式READMEの記載 / 実機結果 / 判定 / 備考」で記録する。
- 実施結果（**実機結果を根拠にした判定はすべて未検証。Q11参照**）: 記事本文（innovaTopia、2026-08-15、山本達也）から16件の検証可能な主張（A1〜A16）を抽出し、`validation-results.md` に「記事の主張 / 公式READMEの記載 / 実機結果 / 判定 / 備考」で記録した。最終的にA1・A2・A3・A5・A6・A7・A8・A10・A11・A12・A13・A14・A15・A16の**14件**が実機で一致。残る2件はA4（MCP AppsのUI描画。対話的なブラウザ確認が必要で未検証）とA9（フェーズ1/2基準日。調査方法論に関する主張のためPhase 0のスコープ外）。Chrome内蔵AIの実操作は記事のA番号ではなく公式README由来の確認項目として未確認のまま残った（Q10参照）。

## 2026-08-17 Phase 0実機検証（Step 0〜8）で新たに判明した要判断事項

### Q9. `claude -p --allowedTools` によるツール制限が完全にサンドボックスできなかった件への対応
- 状態: 解決済み（2026-08-17）— 対応はPhase 1へ繰り延べ
- 背景: Step 8のMCP接続確認で、`--allowedTools` に4つのMCPツールのみを指定して非対話Claude Codeセッションを起動したが、実際には冒頭で `Bash`（`ls -la`）も実行された。これは現在の対話セッションの権限設定（Auto Mode等）を新規セッションが引き継いでいるためと推測される。今回は実害なし（無害なディレクトリ一覧のみ）だった。
- 決定: **Phase 1以降の検討事項とする。** 今回は実害がなく、Phase 0の完了を妨げない。Phase 1で外部MCPサーバーを検証用サブセッションから叩く運用を組む段階で、`--permission-mode` の指定や隔離手段（専用作業ディレクトリ、専用の権限設定ファイル）を決める。
- 申し送り: 今後より機密性の高いディレクトリで同種の検証を行う場合、`--allowedTools` だけでは新規セッションを厳密にMCPツールのみへ制限できないことを前提にする。

### Q10. 未確認のまま残っている4項目をどう扱うか
- 状態: **保留に差し戻し（2026-08-17、Q11参照）**。「他の項目は実測済み」という前提が崩れたため、以下の決定は無効
- 背景: `validation-results.md`の「未確認のまま残っている項目」に記載の4件（MCP Apps UIのグラフ表示／Chrome内蔵AIの実操作／MCP仕様バージョンの`/health`確認／Windows固有問題の網羅性）は、対話的なブラウザ操作やHTTPモード起動など、今回のCLI/非対話MCP検証だけでは確認できなかった。
- 決定: **4項目を「実機で確認困難」として完了条件から除外し、記録のみとする。** Phase 0の目的である「記事の説明と公式実装が実機で一致するか」は、CLI・dataset.yaml・MCP 4ツール・Claude Code経由の実接続で十分に確認できており、残る4項目はいずれもこの判断を覆すものではない。Phase 1で必要になった時点で改めて扱う。VALIDATION_PLAN.md 第5節に反映済み。
- 補足: フェーズ1・2の基準日（A9）は、そもそもデジタル庁の調査そのものの方法論に関する主張であり、MCP実装の検証対象ではないためスコープ外とした（上記4項目とは別枠）。

## 2026-08-17 実行証跡の監査による差し戻し

### Q11. `validation-results.md` の記録に一次証跡がない
- 状態: 判明（2026-08-17）／対応方針は決定済み
- 内容: Phase 0「実機検証」の記録を裏付ける証跡が一つも見つからず、記録された git / uv / Python / Chrome のバージョンが4項目とも実機と不一致だった。詳細は `EVIDENCE_AUDIT.md`。
- 対応: Phase 0 の完了判定を撤回し未完了に戻した。`note-draft-phase0.md` は公開凍結。再検証では実行ログの原文を必須とする（VALIDATION_PLAN.md 第5節「記録の必須要件」）。
- 再検証の結果（同日実施、`logs/`）: **旧記録のデータ系の数値はすべて再現した**（75,071件／3,203KB／相続1,614件／25グループ／13,645 など）。したがって「実行していなかった」という推論は誤りで、コマンドはどこかで実際に実行されたと考えられる。**一方、旧記録の「実行環境」節は実機と一致しない**という問題は残る。旧記録は一次資料として扱わない。
- 教訓: 数値が正しくても、環境の記載が事実と違いログが残っていなければ、第三者は検証できない。検証環境を消す前にログを保存する。

### 差し戻した判断

- **Q3（空き容量の閾値）**: 保留に戻す。根拠としていた「実測1GB未満」に証跡がないため。再検証で実測してから決める。
- **Q10（未確認4項目の切り分け）**: 保留に戻す。「他の項目は実測済みだから残り4項目は落としてよい」という前提が崩れたため。

### 有効なまま維持する判断

Q4・Q5・Q6・Q9は実測結果に依存しない方針判断のため、決定を維持する。

## 判断待ち

- Q3（再検証後に実測して決める）
- Q10（再検証後に、実際に確認できた範囲を見てから決める）
