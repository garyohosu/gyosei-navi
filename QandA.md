# QandA

VALIDATION_PLAN.md レビュー時の不明点・要判断事項。公式リポジトリ digital-go-jp/administrative-procedures-mcp のREADME（2026-08-17時点）と照合済み。即修正できた項目はVALIDATION_PLAN.mdへ反映済みで、ここには含めない。

## 2026-08-17 VALIDATION_PLAN.md レビュー

### Q1. Claude Codeから公式MCPへ接続する前に、`.mcp.json` / `setup.sh` / サーバーコードを読むレビュー工程を入れるか
- 状態: 解決済み（2026-08-17）
- 決定: 実行前レビューを行う。Step 0で `setup.sh`、`.mcp.json`、そこから起動されるコマンド、主要処理、データ取得先を実行前に確認する。WORK_INSTRUCTION.mdに反映済み。

### Q2. 検証結果の記録ファイル名を確定するか
- 状態: 解決済み（2026-08-17）
- 決定: `validation-results.md` をgyosei-naviリポジトリ直下に作成し記録する。WORK_INSTRUCTION.mdに反映済み。

### Q3. 「十分な空き容量」の具体的な閾値
- 状態: 実測により参考値を得た（2026-08-17）
- 実測: `uv sync --extra excel` 後の依存パッケージ（polars-runtime含む）約数百MB、取得データ（xlsx原本14.0MB＋Parquet 3.2MB）を合わせても1GB未満で収まった。検証時のC:ドライブ空き容量は約44GBあり全く問題にならなかった。目安として「2〜3GB程度の空きがあれば十分」としてよいと考えられるが、正式な閾値としてVALIDATION_PLAN.mdに明記するかは引き続きQ3として残す（重大度: 低）。

### Q4. 停止条件にある「代替環境」の範囲
- 重大度: 低
- 背景: 第6節の停止条件で「Windows固有の問題が未解決で、代替環境でも検証できない」とあるが、「代替環境」がWSL/Git Bashまでを指すのか、別マシン（Mac/Linux実機・VM）まで含むのかが未定義。
- 決まれば: 第6節または第4節Step 1に代替環境の範囲を明記する。

### Q5. Step 6の質問例を公式READMEの利用例に寄せるか
- 重大度: 低（任意）
- 背景: 公式READMEには「オンライン未実施の手続にはどんな傾向があるか」「優先的に見直し候補になりそうな行政手続を探して」等の利用例が別途載っている。VALIDATION_PLAN.mdのStep 6は独自の4問のみ。記事の再現性を高めるなら公式例も試す価値があるが、必須ではない。
- 決まれば: 追加するなら第4節Step 6に質問例を追記する。

### Q6. MCP仕様2026-07-28（FastMCP 4系プレリリース）での動作確認も範囲に含めるか
- 重大度: 低（情報不足のため保留寄り）
- 背景: 公式READMEには既定の2025-11-25に加え、プレリリースのFastMCP 4系を追加インストールすれば2026-07-28仕様でも動く旨の記載がある。Phase 0の目的は「記事と公式実装の一致確認」であり、記事執筆時点の既定構成（3系・2025-11-25）で十分と考えられるが、明示的な範囲指定がVALIDATION_PLAN.mdにない。
- 決まれば: 対象外とするなら第2節に「MCP 2026-07-28／FastMCP4系プレリリースは対象外」と明記し、対象に含めるならStep 1にFastMCP4系プレリリース導入手順を追加する。

### Q7. `apcli` コマンドの実行方法（venv activate か `uv run apcli` か）を統一するか
- 状態: 解決済み（2026-08-17）
- 決定: `uv run apcli ...` に統一する。WORK_INSTRUCTION.mdの全コマンド例に反映済み。

### Q8. innovaTopia記事側の主張を先に列挙するか
- 状態: 解決済み（2026-08-17）
- 決定: 実機検証前に記事本文から検証可能な主張を抽出し、公式READMEの記載と区別して記録する。最低限の照合対象10項目をWORK_INSTRUCTION.mdに列挙済み。各項目は「記事の主張 / 公式READMEの記載 / 実機結果 / 判定 / 備考」で記録する。
- 実施結果: 記事本文（innovaTopia、2026-08-15、山本達也）から16件の検証可能な主張（A1〜A16）を抽出し、`validation-results.md` に「記事の主張 / 公式READMEの記載 / 実機結果 / 判定 / 備考」で記録した。うちA1・A2・A3・A6・A7・A8・A10・A11・A14・A15・A16の11件は実機で一致を確認。A4（MCP AppsのUI描画）・A9（フェーズ1/2基準日）・Chrome内蔵AIの実操作は対話的なブラウザ確認が必要で未確認。

## 2026-08-17 Phase 0実機検証（Step 0〜8）で新たに判明した要判断事項

### Q9. `claude -p --allowedTools` によるツール制限が完全にサンドボックスできなかった件への対応
- 重大度: 中
- 背景: Step 8のMCP接続確認で、`--allowedTools` に4つのMCPツールのみを指定して非対話Claude Codeセッションを起動したが、実際には冒頭で `Bash`（`ls -la`）も実行された。これは現在の対話セッションの権限設定（Auto Mode等）を新規セッションが引き継いでいるためと推測される。今回は実害なし（無害なディレクトリ一覧のみ）だったが、今後より機密性の高いディレクトリで同様の検証を行う場合、意図したサンドボックスが効かない可能性がある。
- 決まれば: 今後こうした検証を行う際、`--permission-mode` や別の隔離手段（専用ディレクトリ・専用ユーザー権限設定）を使うかどうかを決める。gyosei-nav側の開発運用としてはPhase 1以降の検討事項でよいか、それとも今すぐ運用ルールを決めるか。

### Q10. 未確認のまま残っている4項目をどう扱うか
- 重大度: 低〜中
- 背景: `validation-results.md`の「未確認のまま残っている項目」に記載の4件（MCP Apps UIのグラフ表示／フェーズ1・2基準日／Chrome内蔵AIの実操作／MCP仕様バージョンの`/health`確認）は、対話的なブラウザ操作やHTTPモード起動など、今回のCLI/非対話MCP検証だけでは確認できなかった。
- 決まれば: これらをPhase 0完了の必須条件として残すか、「実機で確認困難な項目」として完了条件から除外し記録のみで済ませるかを決める。
