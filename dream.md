## 2026-08-17 16:21 Dreamingタイム

### 今回やったこと
- `git pull` を実行し、main を最新状態へ更新した。

### 気づいたこと
- fast-forward で更新され、`memo.md` が変更された。

### 改善点
- 特になし。

### 次に試すとよさそうなこと
- 必要に応じて、今回取り込んだ変更内容を確認する。

## 2026-08-17 16:23 Dreamingタイム

### 今回やったこと
- `memo.md` を元に `VALIDATION_PLAN.md` を作成した。

### 気づいたこと
- 検証は公式MCPの再現を最優先にし、独自開発はPhase 0後に判断する構成が明確になった。

### 改善点
- 実行時の結果は別の検証記録へ残す必要がある。

### 次に試すとよさそうなこと
- 計画に沿って公式リポジトリの環境確認から開始する。

## 2026-08-17 17:10 Dreamingタイム

### 今回やったこと
- `VALIDATION_PLAN.md` をレビューした。公式リポジトリ digital-go-jp/administrative-procedures-mcp のREADMEを実際に取得し、記載内容と突き合わせた。
- 即修正できた点（Python/uv/Chromeの前提バージョン、データ配布元URLとMITライセンス、dataset.yamlを信頼済み設定として扱う注意、`apcli describe` の追記、MCP仕様バージョン確認の追加）をVALIDATION_PLAN.mdへ直接反映した。
- 要判断・情報不足の項目をQandA.mdに8件、残件一覧をFIX_PLAN.mdにまとめた。

### 気づいたこと
- advisorレビューで、自分が追加した項目同士が矛盾していた（2026-07-28仕様を「対象外」と断定しつつQ6で範囲を尋ねていた）ことと、`/health` チェックがStep 6のstdio接続では実行不能だったことを指摘され、修正した。追加した確認項目ほど「本当にその手順で実行できるか」を自分で検証すべきだった。
- `apcli` はvenv内のコマンドのため、venv activateか `uv run apcli` かを決めないとWindowsでStep 2以降がそのまま動かない可能性が高い（Q7）。
- 完了判定の「記事との一致・相違を整理した」は、記事側の主張リストが存在しないと判定不能（Q8）。公式リポジトリ側の事実確認はできたが、記事本文はまだ読んでいない。

### 改善点
- 追加した確認項目は、それを実行する具体的な接続方式（stdio/HTTP）まで踏み込んで整合性を取る。

### 次に試すとよさそうなこと
- QandA.mdのQ1〜Q8についてユーザーの回答を得てからStep 0の実行に進む。特にQ7（apcli実行方法）とQ8（記事側主張の列挙）は着手前に決めておくと手戻りが少ない。

## 2026-08-17 18:40 Dreamingタイム

### 今回やったこと
- Q1/Q7/Q8の決定が反映された `WORK_INSTRUCTION.md` をgit pullで取得し、それに従ってPhase 0の実機検証（Step 0〜8）を最後まで実行した。
- 環境確認、公式リポジトリのclone、`.mcp.json`/`setup.sh`の事前レビュー（不審な処理なしを確認）、依存インストール、実データ取得（75,071件）、CLI検索・集計、dataset.yamlの精読、`apcli describe`によるMCP4ツール確認、`apcli preview`のUI起動確認、Claude Codeからの実際のMCP接続（`claude -p`を`--allowedTools`でMCPツールに限定して2問実行）まで完走した。
- innovaTopia記事本文を実際に取得し、検証可能な主張16件（A1〜A16）を抽出。公式READMEの記載で補完せず記事本文由来として区別し、実機結果と突き合わせて`validation-results.md`に記録した。13件が一致、1件（A9・調査方法論の基準日）はPhase 0のスコープ外と判断、2件（MCP Apps UIのグラフ描画、Chrome内蔵AIの実操作）は対話的ブラウザ操作が必要なため未確認のまま残した。

### 気づいたこと
- Windowsのコンソールコードページ（932）とapcliのUTF-8出力が衝突し日本語が文字化けした。`PYTHONIOENCODING=utf-8`を付けることで解決した。これは記事にも書けるWindows固有の実測情報。
- MCP経由で`summarize_records`にLLMが誤った形式の引数を渡した際、サーバー側の型検証（Pydantic）で明確に拒否され、正しい形式で再試行して成功した。さらにMCP経由の集計結果がCLI直接実行の結果と完全一致した。「AIが間違いやすい計算をサーバー側の決定論的処理へ移す」という記事の核心的主張を、実際の失敗→成功のやりとりと数値一致の両方で裏付けられた。
- `claude -p --allowedTools`で4つのMCPツールのみに絞ったつもりが、実際にはBashも実行されてしまった。現在のセッションの権限設定を新規セッションが引き継ぐためと見られる。実害はなかったが、意図したサンドボックスが効かないことが分かったのは重要な発見。
- advisorから、A3（LLMは条件指定のみ）の主根拠を「サーバー側検証エラー」ではなく「MCP経由とCLI経由の集計結果が完全一致したこと」に置き直すべきと指摘され修正した。エラーが出た＝サーバー側で動いている、と短絡しやすいので、直接の一致証拠を主、エラーは補強、と区別する視点は今後も使う。
- advisorから、A12（スクリプト1本のセットアップ）を「setup.shの内容は読んだが実行していない」状態のまま一致と書きかけていたと指摘された。読むことと実行することは別の検証であり、記事の主張が「動作する」ことである以上、実行して確かめる必要があった。

### 改善点
- 記事の主張を検証する際、「実装コードを読んで矛盾がないか」と「実際に実行して同じ結果が出るか」は別の強さの証拠であることを、記録の時点で区別して書く。
- 非対話の`claude -p`を使った検証はコストと時間がかかる（今回2回で合計約0.65ドル）ので、必要な質問数を絞ってから実行する。

### 次に試すとよさそうなこと
- 残っているQ3・Q4・Q5・Q6・Q9・Q10（`QandA.md`参照）の回答を得る。特にQ9（`--allowedTools`がBashを遮断できなかった件）は今後同様の検証を行う前に方針を決めておきたい。
- 対話的なブラウザでのChrome内蔵AI動作確認とMCP Apps UIのグラフ表示確認をユーザー側で実施してもらう。
- Phase 0はほぼ完了（停止条件には該当せず）なので、note記事の構成案を検討する段階に進めそう。
